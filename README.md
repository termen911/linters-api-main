# Linters API

## Deploy

* Configure `env` varibles
  * ```bash
    cp .env.dist .env
    ```
  * ```bash
    nano .env
    ```

* Run docker-compose
  * ```bash
    docker compose up --build --force-recreate -d
    ```
* Documentation
  * ```bash
    http://x.x.x.x/docs#/
    ```